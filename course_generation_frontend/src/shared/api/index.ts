/** Barrel: `import { post } from "@/shared/api"` or `import apiClient from "@/shared/api/client"`. */
export { default as apiClient, get, post, put, patch, del as delete } from './client'
export * from './types'
