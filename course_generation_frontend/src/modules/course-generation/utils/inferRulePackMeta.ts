// Re-export formatBytes from the global utility for backwards compatibility
// within this module. Consumers outside this module should import from @/utils/formatBytes.
export { formatBytes } from '@/utils/formatBytes';
