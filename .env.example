# Docker Compose (lectora_FE root)
FE_PORT=8080
# Backend reachable from the frontend container (no http:// prefix)
# Host BE on :8000, or use service name if BE runs in lectora-course-gen-engine compose:
BACKEND_HOST=host.docker.internal:8000
# BACKEND_HOST=api:8000
